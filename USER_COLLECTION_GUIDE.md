# 사용자별 컬렉션 격리 기능 가이드

## 개요
이제 각 사용자는 자신만의 개인 컬렉션을 생성하고 관리할 수 있습니다. 컬렉션은 사용자별로 격리되어 다른 사용자의 컬렉션을 볼 수 없습니다.

## 주요 기능

### 1. 컬렉션 타입
- **개인 컬렉션 (👤)**: 각 사용자가 생성한 개인 전용 컬렉션
- **공유 컬렉션 (🌐)**: 모든 사용자가 접근 가능한 공유 컬렉션 (기본 컬렉션)

### 2. 사용자별 격리
- 각 사용자는 자신의 컬렉션만 볼 수 있음
- 다른 사용자의 개인 컬렉션에 접근할 수 없음
- 공유 컬렉션은 모든 사용자가 접근 가능

### 3. 컬렉션 관리
- **생성**: 사용자별로 개인 컬렉션 생성 가능
- **삭제**: 자신의 컬렉션만 삭제 가능
- **전환**: 개인/공유 컬렉션 간 전환 가능

## 설정 방법

### 1. 데이터베이스 마이그레이션 실행
```bash
cd backend
python run_migration.py
```

### 2. 백엔드 서버 재시작
```bash
cd backend
python main.py
```

### 3. 프론트엔드 접속
- 설정 페이지에서 "내 컬렉션 목록" 확인
- 개인 컬렉션과 공유 컬렉션 구분 표시

## 사용 예시

### 컬렉션 생성
1. 설정 페이지 → "새 컬렉션 생성"
2. **컬렉션 타입 선택**:
   - **개인 컬렉션 (👤)**: 나만 접근 가능한 개인 전용 컬렉션
   - **공유 컬렉션 (🌐)**: 모든 사용자가 접근 가능한 공유 컬렉션
3. 컬렉션 이름과 설명 입력
4. 생성된 컬렉션은 선택한 타입에 따라 표시

### 컬렉션 타입 변경
1. 설정 페이지 → "컬렉션 수정" → "컬렉션 타입 변경"
2. 변경할 컬렉션 선택
3. 새 타입 선택 (개인 ↔ 공유)
4. 변경 시 권한 영향 확인 후 적용

### 컬렉션 전환
1. 설정 페이지 → "컬렉션 전환"
2. 개인 컬렉션 또는 공유 컬렉션 선택
3. 선택한 컬렉션으로 RAG 검색 수행

## 보안 고려사항

### 1. 인증 필수
- 모든 컬렉션 API는 사용자 인증 필요
- JWT 토큰을 통한 사용자 식별

### 2. 데이터 격리
- 사용자별 컬렉션 완전 격리
- 다른 사용자 데이터 접근 불가

### 3. 권한 관리
- 자신의 컬렉션만 수정/삭제 가능
- 공유 컬렉션은 읽기 전용

## 문제 해결

### 마이그레이션 실패
```bash
# 데이터베이스 연결 확인
psql -h localhost -p 5433 -U postgres -d postgres

# 수동으로 컬럼 추가
ALTER TABLE langchain_pg_collection ADD COLUMN user_id VARCHAR(255);
```

### 컬렉션 표시 안됨
1. 브라우저 새로고침
2. 로그아웃 후 재로그인
3. 백엔드 서버 재시작

## 기술적 세부사항

### 데이터베이스 스키마
```sql
-- langchain_pg_collection 테이블에 user_id 컬럼 추가
ALTER TABLE langchain_pg_collection 
ADD COLUMN user_id VARCHAR(255);

-- 인덱스 생성
CREATE INDEX idx_langchain_pg_collection_user_id 
ON langchain_pg_collection (user_id);
```

### API 변경사항
- `/api/collections`: 사용자별 컬렉션 반환
- `/api/collections/create`: 사용자 ID 자동 추가
- 인증 토큰을 통한 사용자 식별

### 프론트엔드 변경사항
- 컬렉션 목록에 사용자 정보 표시
- 개인/공유 컬렉션 구분 UI
- 사용자별 컬렉션 관리 인터페이스
