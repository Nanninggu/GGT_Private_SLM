# WebSocket 채팅 시스템 가이드 🚀

HTTP 스트리밍에서 WebSocket 스트림으로 업그레이드된 실시간 채팅 시스템입니다.

## 🆕 새로운 기능

### ✅ WebSocket 스트리밍
- **실시간 양방향 통신**: 서버와 클라이언트 간 지속적 연결
- **더 빠른 응답**: HTTP 요청/응답 오버헤드 제거
- **연결 상태 관리**: 실시간 연결 상태 표시 및 자동 재연결
- **향상된 사용자 경험**: 더 부드러운 스트리밍과 실시간 피드백

### ✅ 하이브리드 모드
- **자동 폴백**: WebSocket 연결 실패 시 HTTP 스트리밍으로 자동 전환
- **투명한 전환**: 사용자는 연결 방식 변경을 인지하지 않음
- **안정성 보장**: 네트워크 문제 시에도 서비스 중단 없음

## 🏗️ 아키텍처

```
Frontend (Vue.js)             Backend (FastAPI)
┌─────────────────┐          ┌─────────────────┐
│ WebSocket Client│◄────────►│ WebSocket Server│
│                 │          │                 │
│ HTTP Fallback   │◄────────►│ HTTP Endpoints  │
└─────────────────┘          └─────────────────┘
```

## 🚀 시작하기

### 1. 백엔드 서버 시작
```bash
cd backend
python main.py
```

### 2. 프론트엔드 시작
```bash
cd vue-frontend
npm run dev
```

### 3. 웹소켓 테스트
```bash
python test_websocket_chat.py
```

## 🔧 주요 변경사항

### 백엔드 (FastAPI)
- **새로운 엔드포인트**:
  - `WS /ws/chat/{session_id}` - WebSocket 채팅
  - `GET /api/websocket/status` - 연결 상태 확인
  - `GET /api/websocket/session/{session_id}` - 세션 상태 확인

- **연결 관리자**: `ConnectionManager` 클래스로 WebSocket 연결 관리
- **실시간 스트리밍**: 청크 단위로 응답을 실시간 전송

### 프론트엔드 (Vue.js)
- **WebSocket 서비스**: `WebSocketService` 클래스로 연결 관리
- **WebSocket 컨트롤러**: `WebSocketChatController`로 채팅 처리
- **연결 상태 UI**: 사이드바에 실시간 연결 상태 표시
- **자동 폴백**: WebSocket 실패 시 HTTP 모드로 전환

## 📊 연결 상태 표시

사이드바에서 다음 상태를 확인할 수 있습니다:

### 🟢 실시간 연결 (WebSocket)
- WebSocket 스트리밍 활성화
- 최적의 성능과 실시간성

### 🟡 HTTP 모드 (폴백)
- WebSocket 연결 실패 시 자동 전환
- 기존 HTTP 스트리밍 사용

### 🔴 백엔드 서버 연결 실패
- 서버가 실행되지 않음
- 연결 확인 필요

## 🎯 사용법

### 1. 일반 채팅
- 메시지 입력 후 Enter 또는 전송 버튼 클릭
- WebSocket이 활성화되면 실시간 스트리밍으로 응답 표시
- 연결 실패 시 자동으로 HTTP 모드로 전환

### 2. RAG 모드
- LangChain RAG 또는 기본 RAG 선택 가능
- 컬렉션별 문서 검색 지원
- 참고 문서 정보 실시간 표시

### 3. 모델 선택
- 다양한 LLM 모델 선택 가능
- 모델별 성능 최적화

## 🔍 문제 해결

### WebSocket 연결 실패
1. **백엔드 서버 확인**: `http://localhost:8002` 접속 가능한지 확인
2. **포트 확인**: 8002번 포트가 사용 중인지 확인
3. **방화벽 설정**: WebSocket 연결을 허용하는지 확인

### 자동 폴백 확인
- WebSocket 연결 실패 시 자동으로 HTTP 모드로 전환
- 사이드바에서 현재 연결 상태 확인 가능

### 성능 최적화
- WebSocket 연결 유지로 더 빠른 응답
- 청크 단위 스트리밍으로 부드러운 사용자 경험

## 📈 성능 비교

| 기능 | HTTP 스트리밍 | WebSocket 스트림 |
|------|---------------|------------------|
| **연결 방식** | 요청/응답 | 지속적 연결 |
| **실시간성** | ⚠️ 제한적 | ✅ 우수 |
| **리소스 사용** | ⚠️ 높음 | ✅ 효율적 |
| **양방향 통신** | ❌ 불가능 | ✅ 가능 |
| **연결 상태 관리** | ❌ 어려움 | ✅ 쉬움 |
| **확장성** | ⚠️ 제한적 | ✅ 우수 |

## 🛠️ 개발자 정보

### WebSocket 메시지 형식
```json
{
  "type": "chunk|context|status|error",
  "content": "응답 내용",
  "finished": false,
  "timestamp": "2024-01-01T00:00:00",
  "model_info": {...},
  "sources": [...]
}
```

### 연결 관리
- 세션별 WebSocket 연결 관리
- 자동 연결 해제 및 정리
- 오류 시 자동 재연결 시도

## 🎉 결론

WebSocket 스트림 채팅 시스템으로 업그레이드하여:
- **더 나은 사용자 경험**: 실시간 스트리밍과 연결 상태 표시
- **향상된 성능**: 효율적인 리소스 사용과 빠른 응답
- **안정성**: 자동 폴백으로 서비스 중단 방지
- **확장성**: 더 많은 동시 사용자 지원 가능

기존 HTTP 스트리밍과 완전 호환되며, 투명하게 업그레이드됩니다!
