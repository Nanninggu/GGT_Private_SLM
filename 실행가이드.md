# Exaone 3.5 2.4 챗봇 실행 가이드

## 🚀 빠른 실행 방법

### 1단계: 가상환경 활성화 (이미 활성화됨)
```bash
# 이미 활성화되어 있습니다!
# 만약 비활성화된 경우: source .venv/bin/activate
```

### 2단계: 백엔드 서버 실행
```bash
# 방법 1: 실행 스크립트 사용 (권장)
./start_backend.sh

# 방법 2: 수동 실행
cd backend && python main.py
```
- 🌐 백엔드 주소: http://localhost:8000
- 📖 API 문서: http://localhost:8000/docs

### 3단계: 프론트엔드 실행 (새 터미널 열기)
```bash
# 방법 1: 실행 스크립트 사용 (권장)
./start_frontend.sh

# 방법 2: 수동 실행
cd frontend/streamlit && streamlit run main.py
```
- 🎨 프론트엔드 주소: http://localhost:8501

## 📁 프로젝트 구조 요약

```
SLM_Python/
├── 📁 backend/              # Python 백엔드 (Layered Architecture)
│   ├── 📁 config/           # ⚙️ 설정 관리
│   ├── 📁 controllers/      # 🎮 API 컨트롤러 (프레젠테이션 계층)
│   ├── 📁 services/         # 🧠 비즈니스 로직 (서비스 계층)
│   ├── 📁 repositories/     # 💾 데이터 접근 (데이터 계층)
│   ├── 📁 models/           # 📋 데이터 모델
│   └── 📄 main.py           # FastAPI 서버 진입점
├── 📁 frontend/streamlit/   # Streamlit 프론트엔드 (MVC 패턴)
│   ├── 📁 controllers/      # 🎮 뷰 컨트롤러
│   ├── 📁 components/       # 🎨 UI 컴포넌트 (뷰)
│   ├── 📁 services/         # 🔗 API 서비스 (모델)
│   └── 📄 main.py           # Streamlit 앱 진입점
└── 📄 README.md             # 프로젝트 문서
```

## 🧪 테스트 방법

1. **백엔드 API 테스트**:
   ```bash
   curl http://localhost:8000/
   ```

2. **프론트엔드 접속**:
   - 브라우저에서 http://localhost:8501 접속
   - 채팅창에 메시지 입력하여 테스트

## 🔧 주요 기능

- ✅ **로컬 SLM 통합**: Exaone 3.5 2.4 모델 (placeholder 구현)
- ✅ **실시간 채팅**: 웹 기반 채팅 인터페이스
- ✅ **세션 관리**: 채팅 기록 저장 및 관리
- ✅ **RESTful API**: FastAPI 기반 백엔드
- ✅ **반응형 UI**: Streamlit 기반 프론트엔드

## ⚠️ 주의사항

1. **Exaone 모델**: 현재는 placeholder 응답을 제공합니다. 실제 Exaone 3.5 2.4 모델을 연결하려면 `backend/services/llm_service.py` 파일을 수정해야 합니다.

2. **포트 충돌**: 8000번(백엔드), 8501번(프론트엔드) 포트가 사용 중이 아닌지 확인하세요.

3. **순서**: 반드시 백엔드를 먼저 실행한 후 프론트엔드를 실행하세요.

## 🛠️ 다음 단계

1. **실제 Exaone 모델 연결**
2. **데이터베이스 연결** (현재는 파일 기반 저장)
3. **사용자 인증** 추가
4. **채팅 기록 내보내기** 기능
5. **모델 설정 UI** 추가
